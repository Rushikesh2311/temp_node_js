const saywalk = (name) => {
    console.log(`${name} is walking.`);
  };

const sayhi = () => {
    console.log('Hi there!');
  };

const sayRun =(name) =>{
  console.log(`${name} is running`);
}
  
module.exports = {saywalk, sayhi , sayRun };
